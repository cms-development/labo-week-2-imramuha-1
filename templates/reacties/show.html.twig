{% extends 'base.html.twig' %}

{% block title %}Reacties{% endblock %}

{% block body %}
    <h1>Reacties</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reacty.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reacties_index') }}">back to list</a>

    <a href="{{ path('reacties_edit', {'id': reacty.id}) }}">edit</a>

    {{ include('reacties/_delete_form.html.twig') }}
{% endblock %}
